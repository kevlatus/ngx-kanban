import { SortColumnDataPipe } from './sort-column-data.pipe';

describe('SortColumnDataPipe', () => {
  it('create an instance', () => {
    const pipe = new SortColumnDataPipe();
    expect(pipe).toBeTruthy();
  });
});
